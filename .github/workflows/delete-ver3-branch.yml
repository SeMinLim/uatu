name: Delete temporary ver3 branch

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  delete-branch:
    if: github.event.head_commit.message == 'chore: remove temporary ver3 branch'
    runs-on: ubuntu-latest
    steps:
      - name: Delete temporary branch
        env:
          GH_TOKEN: ${{ github.token }}
          REPOSITORY: ${{ github.repository }}
        run: |
          curl --fail-with-body --request DELETE \
            --header "Accept: application/vnd.github+json" \
            --header "Authorization: Bearer ${GH_TOKEN}" \
            --header "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${REPOSITORY}/git/refs/heads/agent/add-ver3-clause-retention"
