name: Uatu Major Heuristic Screening Placeholder

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/uatu-major-single-screen.yml

concurrency:
  group: uatu-major-heuristic-screen
  cancel-in-progress: true

jobs:
  defer:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Major heuristic screening is deferred until low-cost screening completes.'
